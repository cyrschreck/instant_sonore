{
  "name": "instant-sonore-site",
  "version": "1.0.0",
  "description": "L'Instant Sonore — site web + CMS léger",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve public",
    "watch": "node build.js && echo 'Modifie content.json puis relance npm run build'"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
